version: "2.2"
services:
  frontend:
    # uncomment for local dev
#    build: ./
    restart: always
    image: gitlab.zbmed.de:5050/km/semlookp/widgets-semlookp:${VERSION}
    ports:
      - ${PORT}:80
